<div id="<%= dom_id user %>" class="scaffold_record">
  <h2>
    <strong>
      <%= user.first_name %> <%= user.last_name %>
    </strong>
  </h2>

  <p>
    <strong>Phone number:</strong>
    <%= user.phone_number %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= user.email %>
  </p>
</div>
